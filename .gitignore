build/bin/
build/deb/
frontend/dist/
frontend/node_modules/
frontend/package.json.md5

# Claude status monitoring: project-level hook config is generated per-machine
# (the hook command embeds the local todoai executable path), so keep it local.
.claude/settings.json

# local `go build` artifact
/todoai.exe
