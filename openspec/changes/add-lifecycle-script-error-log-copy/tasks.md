## 1. 后端完整失败输出生命周期

- [x] 1.1 为 `TodoLifecycleScriptExecutor` 增加失败输出测试，覆盖超过 4096 字节的完整输出保留、摘要继续截断、重试替换、成功清理、按 TODO 清理和全部清理
- [x] 1.2 在执行器中实现受互斥锁保护的完整失败输出存储、仅限当前失败状态的查询接口，以及与 `Start`、成功、`Clear`、`ClearTodo` 和 workspace 变更一致的清理逻辑
- [x] 1.3 为应用层按需读取 API 增加测试，覆盖初始化脚本和完成脚本失败、无进程输出时回退错误信息、无效阶段、状态已失效及 workspace 切换
- [x] 1.4 实现按 `todoId` 和 `phase` 读取当前生命周期脚本完整错误输出的 Wails API，并在 workspace 打开、切换和关闭时清除旧运行时日志

## 2. Wails 绑定与前端数据流

- [x] 2.1 更新 Wails 生成的 JavaScript/TypeScript 绑定和前端 API mock，使客户端能够调用完整错误输出读取 API
- [x] 2.2 在 `App.vue` 中实现按失败状态键加载和缓存完整输出、迟到响应校验、状态变化清理，以及使用 `ClipboardSetText` 原样复制和沿用全局错误提示
- [x] 2.3 扩展 `ProjectSidebar` 的 props 与事件契约，使失败状态可以请求完整输出和触发复制，同时保持普通状态只展示现有截断摘要

## 3. 失败日志交互

- [x] 3.1 在失败状态的 Retry 按钮旁增加带可访问名称和提示的 Copy 图标按钮，确保运行中和成功状态不显示该按钮
- [x] 3.2 沿用现有 600ms 悬浮详情模式实现失败日志浮层，保留原始换行，使用等宽字体、稳定尺寸和滚动区域，并在离开、重试或状态失效时关闭
- [x] 3.3 增加客户端自动化测试，覆盖开始脚本与完成脚本失败、超过 4096 字节的完整悬浮内容、精确剪贴板内容、空输出回退、复制失败反馈和 Retry 后旧日志清理

## 4. 验证与交付

- [x] 4.1 运行 Go 格式化和后端测试，确认完整输出查询、清理以及现有生命周期脚本执行和 Retry 行为通过验证
- [x] 4.2 在 `frontend` 目录运行 `npm test` 和 `npm run build`，确认客户端自动化测试及生产构建通过
- [x] 4.3 执行自动代码审查，检查并发访问、workspace 隔离、异步响应竞态、剪贴板错误处理、可访问性和日志大文本渲染风险，并修复发现的问题
- [x] 4.4 运行 `wails build -tags webkit2_41` 生成可执行文件并确认打包成功

## 5. 启动恢复回归修复

- [x] 5.1 增加应用启动自动恢复 workspace 后点击开始的回归测试，复现生命周期执行器仍为空作用域的问题
- [x] 5.2 在 workspace 恢复成功时同步生命周期运行时作用域，确保 TODO 返回执行中并启动初始化脚本
- [x] 5.3 重新运行相关测试、完整后端测试、前端测试和 Wails 打包验证

## 6. 错误悬浮窗定位回归修复

- [x] 6.1 增加失败状态靠近视口底部时的定位测试，验证短日志使用悬浮窗真实高度紧贴触发行上方显示，并覆盖下方空间充足时的优先定位
- [x] 6.2 在完整日志悬浮窗挂载后测量触发行与浮层真实尺寸，按下方优先、空间不足翻转、视口安全边距限制的规则计算位置
- [x] 6.3 运行生命周期悬浮聚焦测试、前端全量测试和生产构建，并复审定位逻辑与现有延迟、Retry 和 workspace 清理行为
