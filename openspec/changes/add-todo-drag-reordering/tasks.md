## 1. 工作区 UI 状态与后端 API

- [x] 1.1 先为 `TodoProjectUIStateStore` 增加失败测试，覆盖 v0/v1 文件迁移、默认优先级模式、两个状态顺序的去重过滤、缺失 ID 追加和工作区隔离。
- [x] 1.2 将工作区 UI 状态升级为 v2，新增有类型的 `TodoManualOrders`、排序模式规范化及基于当前 TODO 集合的顺序规范化逻辑，同时保留侧栏宽度和 TODO project 视图数据。
- [x] 1.3 先为 App 层列表 UI 状态接口增加失败测试，覆盖非法模式、未知或状态不匹配 ID、原子保存以及保存失败不破坏旧文件。
- [x] 1.4 实现工作区级 `SaveTodoListUIState` 和规范化加载流程，使模式与两组顺序一次保存并返回后端确认后的状态。
- [x] 1.5 验证现有 `SaveTodoProjectUIState`、`SaveTodoSidebarWidth` 和删除 UI 状态操作不会覆盖新增字段。

## 2. App 前端状态与持久化流程

- [x] 2.1 先在 `App.test.js` 中增加失败测试，覆盖工作区加载恢复模式与顺序、首次进入手动模式快照当前显示顺序、工作区切换隔离和旧状态默认值。
- [x] 2.2 在 `App.vue` 中持有并规范化 `todoSortMode`、`todoOrders`、初始化标志和保存中状态，并把对应属性及事件接入 `ProjectSidebar`。
- [x] 2.3 实现排序模式与顺序的乐观保存流程：成功时采用后端返回状态，模式保存失败时恢复原模式，拖拽保存失败时恢复原顺序并显示现有错误提示。
- [x] 2.4 在 TODO 创建、删除和状态变化后重新协调两个开放列表，使新建 TODO 和进入目标状态的 TODO 追加到列表末尾，其他 TODO 相对顺序保持不变。

## 3. 手动排序与拖拽交互

- [x] 3.1 添加 `sortablejs` 前端依赖并更新 lockfile，封装可在组件测试中替换的 SortableJS 初始化和销毁边界。
- [x] 3.2 先在 `ProjectSidebar.test.js` 中增加失败测试，覆盖第三种手动模式、手动 rank 排序、两个状态独立顺序、已完成列表不受影响及拖拽手柄的显示条件。
- [x] 3.3 扩展 `ProjectSidebar.vue` 的排序控件与计算属性，在手动模式按有序 ID 渲染，并使用带可访问名称和 tooltip 的 Lucide 拖拽手柄作为唯一触发区域。
- [x] 3.4 先增加拖拽生命周期失败测试，覆盖开始时全部视觉收起、落放和取消后恢复展开状态、无位置变化不保存、跨状态禁止以及卸载清理。
- [x] 3.5 集成 SortableJS 同列表排序、稳定 `data-id`、占位与选中样式、边缘自动滚动，并在非手动模式或保存期间禁用实例。
- [x] 3.6 实现幂等拖拽清理和列表级临时视觉收起，确保不修改既有展开 ID，并在拖动期间关闭浮层、禁用排序切换、批量展开收起及 TODO 行操作。
- [x] 3.7 完善拖拽手柄、占位项、拖动项和收起状态样式，确保侧栏窄宽度、长标题及滚动场景下布局稳定且无文本或控件重叠。

## 4. 绑定与自动化验证

- [x] 4.1 重新生成 Wails JavaScript/TypeScript 绑定和模型，确认列表 UI 状态加载、保存 API 及新增字段可被前端类型安全调用。
- [x] 4.2 补充 Go 集成测试，覆盖重新打开工作区后的顺序恢复、不同工作区独立状态以及旧 UI 状态文件的实际读写迁移。
- [x] 4.3 补充客户端自动化测试，覆盖拖拽保存成功与失败回滚、新建和状态切换追加、删除清理、保存期间交互锁定、优先级/时间排序回归及已完成排序回归。
- [x] 4.4 运行 `gofmt`、`go test ./...`、`cd frontend && npm run test` 和 `cd frontend && npm run build`，修复所有格式、测试和构建问题。
- [x] 4.5 在 Wails/Vite 运行环境中人工验证真实拖动、全部视觉收起与恢复、长列表自动滚动、工作区重开恢复以及保存失败提示。

## 5. 审查与打包

- [x] 5.1 运行 OpenSpec 严格校验，确认实现与 `proposal.md`、`design.md` 和 `todo-workspace` delta spec 一致。
- [x] 5.2 执行自动化代码审查，重点检查 UI 状态 v2 迁移、异步保存竞态、拖拽取消与卸载清理、可访问性、测试覆盖和生成绑定，并修复审查发现。
- [x] 5.3 运行 `wails build -tags webkit2_41`，生成并验证可执行文件。
