## 1. 后端工作区状态

- [x] 1.1 扩展持久化模型，新增 `Todo`、`TodoProject`、归档项目快照、父目录导入摘要和 TODO 级活动上下文字段。
- [x] 1.2 更新配置加载逻辑，兼容旧 `projects.json`，为缺失的 TODO 字段提供默认值，并保持已有项目库数据可读。
- [x] 1.3 实现 TODO 创建、选择、项目关联去重、完成归档、删除归档和归档快照生成逻辑。
- [x] 1.4 实现父目录批量导入，扫描第一层可访问子目录，跳过文件、不可访问目录和重复路径，并返回导入摘要。
- [x] 1.5 更新项目删除逻辑，从活动 TODO 关联中移除项目，保留归档快照，并返回一致的工作区状态。
- [x] 1.6 为工作区状态管理补充 Go 单元测试，覆盖旧配置加载、TODO 持久化、多 TODO 关联同一项目、归档快照、父目录批量导入和项目删除。

## 2. Shell 运行态隔离

- [x] 2.1 扩展 `ProjectTerminal` 运行态数据，加入 `todoId` 和 `todoProjectId`，并调整排序和序列化字段。
- [x] 2.2 将 `ShellSessionManager` 的活动终端索引从项目级调整为 TODO 项目上下文级。
- [x] 2.3 更新创建、确保、选择和删除终端逻辑，使终端只能在 TODO 项目上下文中创建和复用。
- [x] 2.4 实现按 TODO 批量关闭终端，以及按项目跨 TODO 上下文批量关闭终端。
- [x] 2.5 更新启动配置命令执行逻辑，确保新终端归属到选中的 TODO 项目上下文。
- [x] 2.6 为 shell 运行态补充 Go 单元测试，覆盖同一项目在两个 TODO 下终端隔离、删除 TODO 清理终端、删除项目跨 TODO 清理终端和删除终端后的活动选择。

## 3. Wails API 和绑定

- [x] 3.1 新增或调整 App 方法，用于加载工作区状态、创建 TODO、关联项目、选择 TODO 项目、完成 TODO、删除 TODO、父目录批量导入和 TODO 级终端创建。
- [x] 3.2 调整现有项目选择 API，使项目 tab 选择项目不会自动创建或激活终端。
- [x] 3.3 调整 Git 状态查询入口，使当前 Git 状态来自选中的 TODO 项目上下文或项目库选中项目，并在无上下文时显示空状态。
- [x] 3.4 重新生成 Wails 前端 bindings，更新 `frontend/wailsjs/go/main/App.js`、`App.d.ts` 和 `models.ts`。
- [x] 3.5 为 App 公共 API 补充 Go 测试，覆盖 TODO 工作流、项目 tab 管理行为和父目录导入摘要。

## 4. 前端状态和交互

- [x] 4.1 更新 `App.vue` 状态结构和 `applyState`，接入 TODO、TODO 项目关联、归档列表、导入摘要和 TODO 级活动终端。
- [x] 4.2 将侧边栏拆为 TODO tab 和项目 tab，保留项目库导入/删除能力，并移除项目 tab 的终端入口。
- [x] 4.3 在 TODO tab 实现活动 TODO 树，按 `TODO -> 项目 -> 终端` 展示，支持展开收起、选择 TODO 项目、选择终端和终端活动指示。
- [x] 4.4 实现 TODO 创建和项目关联流程，允许同一项目关联多个 TODO，并避免同一 TODO 内重复关联。
- [x] 4.5 在 TODO 项目行实现终端启动菜单，复用内置 `Terminal` 和已配置启动配置。
- [x] 4.6 实现完成 TODO 和删除 TODO 的确认流程，成功后清理终端 UI 状态并显示归档结果。
- [x] 4.7 在 TODO tab 中实现归档查看，展示归档原因、归档时间和项目快照，并确保查看归档不会启动 shell。
- [x] 4.8 实现父目录批量导入入口和导入摘要展示。
- [x] 4.9 更新终端面板空状态、工作区标题和状态栏文案，使其反映 TODO 项目上下文。

## 5. 前端测试

- [x] 5.1 更新或拆分 `ProjectSidebar` 测试，覆盖 TODO/项目 tab 切换、项目 tab 无终端入口、TODO 树层级和展开收起。
- [x] 5.2 添加 TODO 交互测试，覆盖创建 TODO、关联项目、同一项目出现在多个 TODO、选择 TODO 项目和选择终端。
- [x] 5.3 添加归档视图测试，覆盖完成/删除 TODO 后归档显示、项目快照保留和不启动终端。
- [x] 5.4 更新 App 测试 mock，覆盖父目录导入摘要、项目选择不创建终端、TODO 级终端创建和终端清理后的 UI 状态。

## 6. 验证

- [x] 6.1 运行 `go test ./...` 并修复失败。
- [x] 6.2 运行 `cd frontend && npm run test` 并修复失败。
- [x] 6.3 运行 `cd frontend && npm run build` 并修复失败。
- [x] 6.4 如果生成了 Wails bindings，检查生成文件已随后端 API 一致更新。
- [ ] 6.5 手动验证核心路径：父目录批量导入、创建 TODO、关联项目、同项目多 TODO 终端隔离、完成 TODO 归档、删除项目跨 TODO 清理。
