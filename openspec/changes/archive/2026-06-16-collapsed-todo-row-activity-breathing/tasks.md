## 1. 前端状态渲染

- [x] 1.1 调整 `ProjectSidebar` 中折叠 TODO 的活动状态渲染，保留行级语义状态但移除 TODO 行上的终端活动小图标。
- [x] 1.2 将折叠 TODO 的聚合活动状态传递到覆盖 TODO item 整条 header 的元素上，确保展开控件、标题区域和操作区域共享状态反馈。
- [x] 1.3 保持展开 TODO 时仅由具体终端行展示活动图标和活动状态。

## 2. 样式与可访问性

- [x] 2.1 为折叠 TODO 的 `busy` 状态添加较轻的整行呼吸样式。
- [x] 2.2 为折叠 TODO 的 `needs-input` 状态添加更醒目且与 `busy` 可区分的整行呼吸样式。
- [x] 2.3 添加 `prefers-reduced-motion` 处理，降低或取消持续呼吸动画但保留静态状态提示。
- [x] 2.4 验证呼吸样式在高、中、低优先级 TODO header 背景上保持文本和按钮可读。

## 3. 测试与验证

- [x] 3.1 更新 `ProjectSidebar` 组件测试，断言折叠 TODO 使用行级状态反馈且不再渲染 `todo-activity-*` 图标。
- [x] 3.2 保留或补充 `needs-input` 高于 `busy` 的折叠 TODO 聚合优先级测试。
- [x] 3.3 保留或补充展开 TODO 时终端行继续显示自身活动图标、TODO 父级不显示聚合活动反馈的测试。
- [x] 3.4 运行客户端自动化测试，确认 TODO 工作区和终端活动状态相关测试通过。

## 4. 质量检查与打包

- [x] 4.1 执行自动 review，检查实现是否符合 proposal、design、spec 和现有代码风格。
- [x] 4.2 修复自动 review 或测试发现的问题，并重新运行受影响验证。
- [x] 4.3 运行 `wails build -tags webkit2_41` 生成可执行文件。
