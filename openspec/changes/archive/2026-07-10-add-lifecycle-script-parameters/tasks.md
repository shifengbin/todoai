## 1. 后端数据模型与校验

- [x] 1.1 为生命周期脚本模板和 TODO 脚本快照增加参数定义、参数值字段，并保持缺省为空列表/空映射
- [x] 1.2 实现参数定义规范化与校验，包括参数名格式、同一模板内重复参数名、必填参数创建 TODO 校验
- [x] 1.3 更新 settings 和 project 持久化测试，覆盖参数定义保存、无参数旧数据兼容、TODO 参数快照不受模板修改影响

## 2. 脚本参数渲染与执行

- [x] 2.1 在生命周期脚本执行请求中携带参数定义和值，并在启动 init/complete/retry 时使用 TODO 快照参数
- [x] 2.2 实现 `{{param_name}}` 占位符渲染，仅替换已声明参数，未声明占位符保持原样
- [x] 2.3 实现 bash/zsh/fish/sh、PowerShell/pwsh、cmd 的字符串字面量转义函数
- [x] 2.4 增加脚本渲染和执行请求单元测试，覆盖空值、空格、引号、shell 特殊字符、未知占位符和重试复用快照参数

## 3. 前端表单与 Wails 类型

- [x] 3.1 更新 Wails 生成模型和前端脚本 clone/snapshot 逻辑，传递参数定义和参数值
- [x] 3.2 在脚本管理弹窗中增加参数列表编辑、增删、必填开关、默认值和校验错误展示
- [x] 3.3 在创建 TODO 表单选择脚本后展示参数输入项，使用默认值初始化，切换脚本时重建参数值
- [x] 3.4 在提交创建 TODO 前校验必填参数，并将参数值写入 lifecycleScript 快照请求
- [x] 3.5 在脚本管理和创建 TODO 参数区域增加问号悬浮帮助，说明使用方法和注意事项
- [x] 3.6 优化脚本管理列表为默认单行摘要、点击展开编辑，并支持参数区域独立折叠

## 4. 验证与质量检查

- [x] 4.1 增加或更新客户端自动化测试，覆盖脚本参数编辑、选择脚本后参数输入、必填参数校验和提交 payload
- [x] 4.2 运行后端测试 `go test ./...`
- [x] 4.3 运行客户端测试 `npm --prefix frontend test`
- [x] 4.4 执行自动 code review，检查实现是否满足 proposal/design/specs 且没有引入无关改动
- [x] 4.5 运行 Wails 打包命令 `wails build -tags webkit2_41`
