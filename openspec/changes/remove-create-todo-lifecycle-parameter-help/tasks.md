## 1. 创建 TODO 表单调整

- [x] 1.1 从创建 TODO 的生命周期脚本参数标题区域移除专用问号按钮及其悬浮、焦点事件绑定，同时保留参数标题、参数字段和全局脚本管理共用的帮助逻辑。

## 2. 客户端自动化测试

- [x] 2.1 更新 `frontend/src/App.test.js`，断言创建 TODO 选择含参数的脚本后不显示参数帮助问号，并继续覆盖参数展示、默认值、必填校验和提交行为。
- [x] 2.2 保留并验证全局生命周期脚本管理的参数帮助浮层测试，确认共享帮助功能未受影响。
- [x] 2.3 在 `frontend` 目录运行 `npm test`，确保客户端自动化测试全部通过。

## 3. 自动审查

- [x] 3.1 对代码和测试差异执行自动化代码审查，确认变更仅影响创建 TODO 的帮助入口、符合增量规范，并修复发现的问题。

## 4. 应用打包

- [x] 4.1 运行 `wails build -tags webkit2_41`，生成可执行文件并确认打包成功。
