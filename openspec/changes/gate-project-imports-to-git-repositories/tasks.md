## 1. 后端导入能力

- [x] 1.1 增加路径自身是否为 Git 仓库的判定 helper，覆盖 `.git` 目录和 `.git` 文件场景。
- [x] 1.2 调整单个项目导入接口，使其在所选目录不是 Git 仓库时返回可确认初始化的导入状态，而不是直接保存候选。
- [x] 1.3 增加确认初始化后的单个导入后端流程，在所选目录执行 Git 初始化并成功后保存候选项目。
- [x] 1.4 调整父目录批量导入逻辑，只扫描一级子目录并只导入自身为 Git 仓库的子目录，非 Git 子目录计入跳过摘要。
- [x] 1.5 保持导入后不立即触发 active project Git 状态刷新。

## 2. 前端交互

- [x] 2.1 调整单个项目导入流程，遇到非 Git 目录时提示用户是否初始化 Git 仓库后导入。
- [x] 2.2 用户确认初始化时调用后端初始化导入流程，并沿用现有导入后选择新候选项目的行为。
- [x] 2.3 用户拒绝初始化时不导入项目，并显示 `只能导入 Git 仓库` toast。
- [x] 2.4 增加独立 toast 状态和样式，toast 显示 2 秒后自动消失，重复触发和组件卸载时清理定时器。
- [x] 2.5 为所有批量导入按钮添加 hover 提示 `仅导入一级子目录中的 Git 仓库`。

## 3. 自动化测试

- [x] 3.1 添加 Go 单元测试，覆盖 Git 仓库判定 helper 的 `.git` 目录、`.git` 文件和非 Git 目录。
- [x] 3.2 添加 Go 单元测试，覆盖单个非 Git 导入返回初始化确认状态、确认后初始化并导入、拒绝时不导入的后端路径。
- [x] 3.3 添加 Go 单元测试，覆盖批量导入只导入一级 Git 仓库子目录并跳过非 Git 子目录。
- [x] 3.4 添加前端自动化测试，覆盖单个非 Git 导入确认初始化后继续导入。
- [x] 3.5 添加前端自动化测试，覆盖拒绝初始化时显示 toast 且 2 秒后自动消失。
- [x] 3.6 添加前端自动化测试，覆盖批量导入按钮 hover 提示文案。

## 4. 验证与交付

- [x] 4.1 运行 Go 测试，确认后端导入和 Git 相关测试通过。
- [x] 4.2 运行客户端自动化测试，确认 Vue 交互和 toast 测试通过。
- [x] 4.3 执行自动 review，检查实现是否符合 OpenSpec、现有架构边界和代码规范。
- [x] 4.4 运行 `wails build -tags webkit2_41`，生成可执行文件。
